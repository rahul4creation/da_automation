# DA Automation UI Live Primary

This folder is the primary live runtime UI served locally at:

```text
http://127.0.0.4:5290/
```

It was originally copied from:

```text
D:\AIReview\da_automation\DA AUTOMATION UI\release-runtime\win-unpacked\resources\app
```

Run it with:

```powershell
npm start
```

The committed package includes the runtime server and built `dist` UI bundle. Local runtime folders such as `.env`, `node_modules`, `tmp`, `projects`, and `projects-live-copy` are intentionally ignored.
